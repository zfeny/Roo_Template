# Automations (Agent-Triggered)

本目录仅保留“对话内触发”的自动化定义，不要求用户手动执行终端命令。

- Review Gate 引擎：`../scripts/review_gate.py`
- 触发方式：在 Roo 对话中切换 Reviewer/Validator 模式，由 agent 自动调用。

legacy Bash / hook / make 资产已迁入：
- `.roo_process/archive/legacy_automations_snapshot/`
